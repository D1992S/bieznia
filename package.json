{
  "name": "mozetobedzieto",
  "version": "0.0.1",
  "private": true,
  "description": "Analityczna maszyna AI dla twórców YouTube",
  "type": "module",
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "node scripts/dev-desktop.mjs",
    "dev:ui": "pnpm --filter @moze/ui run dev -- --host 127.0.0.1 --port 5173",
    "dev:desktop": "pnpm --filter @moze/desktop run dev"
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "packageManager": "pnpm@10.29.2",
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@types/node": "^25.2.3",
    "eslint": "^9.39.2",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.55.0",
    "vitest": "^4.0.18",
    "zod": "^4.3.6"
  }
}
